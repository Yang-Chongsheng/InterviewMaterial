|  题库大类 | 题目  |  答案 |
|---|---|---|
|  算法01 |  两个有序数组的合并 |  两个指针指向数组头部，同时移动选出合适元素，添加指新的数组 |
|  算法02 | 一个文件里有9998个数，对应[1-10000]范围，少了两个数，假设内存只有1k，怎么找到是哪两个数少了  |  读一遍，计算“和”与“平方和”，然后算1+2+...+10000，`1^2+2^2+...+10000^2`，得出x+y和`x^2+y^2`的值，再计算x，y二元方程 |
|  算法03 |  用1G内存对一个每行只有一个数字的1T文件排序 | 先取一部分找出范围，分到多个子文件中（每个子文件的范围是固定的），然后单独对多个子文件排序后连接在一起。用一个超大的数组，数组的大小是 max-min，然后 遍历有就标记一下  |
|  算法04 | 10亿个无序数字中，找出重复的数字  |  桶排序，分治排序； |
|  算法05 |  实现快速排序 |   |
|--- |  --- | ---  |
|  基础01 |  介绍5种常见http状态码 |  1xx：消息，2xx：成功，3xx：重定向，4xx：请求错误，5xx：服务器错误 |
|  基础02 |   MySQL数据库事务的隔离级别 |  Read Uncommitted(读取未提交内容)、Read Uncommitted(读取未提交内容)、Repeatable Read(可重读)、Serializable(可串行化) |
|  基础03 |   tcp/ip建立连接和关闭连接的过程 |  三次握手和四次握手，状态转移图 |
|  基础04 |   查看文件file.dat的第2行的内容 |  sed -n '2p' file.dat |
|  基础05 |   统计日志文件中出现次数最多的ip。部分日志文件内容例如: <br>16:20:30 100.69.238.22 "GET /order/get?lang=zh-CN&uid=1 HTTP/1.0"   <br>   16:20:38 100.69.238.11 "GET /order/create?lang=zh-CN&uid=1 HTTP/1.0"<br>   16:20:45 100.69.238.33 "GET /order/send?lang=zh-CN&uid=1 HTTP/1.0" |  使用awk分析 |
|  基础06 |   列出src目录下的所有txt文件名 |  find   .    -name   "*.txt"   -print |
|  基础07 |   Nginx连接PHP-FPM有的方式有哪些 |  使用IP + 端口 或 unix.sock 两种方式 |
|  基础08 |   mysql innodb 和 myisam的区别 |  回答innodb支持事务 行锁 外键 |
|  基础09 |   事务的几种实现方式，innodb采用的方案是 |   |
|  基础10 |   基于上述区别，两引擎分别适用什么场景 |   |
|  基础11 |   如何查看某条sql是否命中了索引 |  explain |
|  基础12 |   Redis的常见数据结构有哪些 |   |
|  基础13 |   Redis的数据过期机制有哪些  |   |
|  基础14 |   mysql 索引最左原则考察  |  联合索引 多列索引 单列索引 |
|  基础15 |    PHP的array内部数据结构是什么，让你设计个支持数组和对象的数据结构如何设计  |  hashmap加双向链表；说出支持插入队列并且能设置kv的数据结构即可 |
|  基础16 |    进程间的通信方式有哪些？其中基于socket的进程通讯中的长、短连接的优缺点及应用场景 |  管道/共享内存/消息队列/socket......短连接常见于大客户情况 如WEB服务器，如果每个连接都使用长连接 那么每个客户都保留一个socket ，系统资源耗费很大 长连接则是多用于操作频繁情况，每个TCP连接都需要三步握手 这需要时间 如果每个操作都是先连接 再操作的话那么处理速度会降低很多 所以每个操作完后都不断开 下次处理时直接发送数据包就OK了，不用建立TCP连接 |
|  基础17 |   redis单机->集群设计  |  考察知识点：分布式存储，rep数据冗余、分片、路由、元数据管理、备份； |
|  基础18 |   用户从浏览器点击到服务器处理PHP代码都做了什么，php-cgi是什么  | DNS、路由器、nginx、php-fpm、zend底层、返回；PHP自带的FastCGI管理器 |
|  基础19 |   使用过哪些RPC协议  |   |
|  基础20 |   redis持久化机制有哪些;  |   |
|  基础21 |   redis 主从同步策略；  |   |
|  基础22 |   乐观锁实现；  |   |
|--- |  --- | ---  |
|  设计01 |   mysql数据表分表方案；  | 分表的目的就在于此，减小数据库的负担，缩短查询时间.可以按照业务的规则来分解为多个子表。<br> 通常为以下几种类型，也可自己定义规则。<br> Range（范围） – 允许将数据划分不同范围。例如通过时间(年、月)分表。<br> Hash（哈希） – 通过对表的一个或多个列的Hash Key进行计算，进行分表。例如对表主键的分表。 <br>List（预定义列表） – 允许系统通过预定义的列表的值来对数据进行分割。<br>Composite（复合模式） –以上模式的组合使用  |
|  设计02 |   订单异步导出；  | 重点考察任务系统的设计。在线导出肯定无法完成，所以改成类似离线的异步任务，考察异步任务的设计。最重要考察一下候选人对产品的设计，比如可以直接问，如果是你怎么和pm改这个交互？  |
|  设计03 |  如何实现一个抢购业务；  | 关注高并发的控制、如何原子性、如何保证大流量在没到达后端时就被抛弃了。控制到达后端的压力在后端的可承受范围；  |
|  设计04 |  200W级mysql一个表 怎么去掉重复的行；  | 通过group by找出"重复标题","重复次数","最小ID"到临时表"tmptitle",然后根据"tmptitle"表中删除原表中的重复行，仅保留ID最小的重复标题行和未重复行。  |


....



系统设计
<a href="https://book.douban.com/subject/36661336/">搞定系统设计：面试敲开大厂的门 (豆瓣)</a>


<a href="https://book.douban.com/subject/36864478/">亿级流量系统架构设计与实战 (豆瓣)</a>
<a href="https://book.douban.com/subject/36403511/">分布式系统架构 (豆瓣)</a>
<a href="https://book.douban.com/subject/1138771/">人本界面 (豆瓣)</a>